#include <stdio.h>

int main() {
    int food = 5, score = 0;
    char move;

    printf("Collect %d food items by pressing 'f'\n", food);
    while(food > 0) {
        printf("Press 'f' to collect: ");
        move = getchar();
        getchar();  // Clear newline

        if (move == 'f') {
            printf("Food collected!\n");
            score++;
            food--;
        } else {
            printf("Wrong key! Try again.\n");
        }
    }
    printf("All food collected! Final Score: %d\n", score);
    return 0;
}
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {
    char move;
    int caught = 0;
    srand(time(0));

    for(int i=0; i<5; i++) {
        printf("A star is falling! Type 'c' to catch: ");
        move = getchar();
        getchar();  // Clear newline
        if (move == 'c') {
            printf("Caught it!\n");
            caught++;
        } else {
            printf("Missed!\n");
        }
    }
    printf("You caught %d stars!\n", caught);
    return 0;
}
*LOGIN PAGE FOR (FOOD COLLECTOR)
#include <stdio.h>
#include <string.h>

int main() {
    char username[20], password[20];
    char stored_username[] = "player1";
    char stored_password[] = "arcade123";

    printf("Welcome to the Game Login Page\n");
    printf("--------------------------------\n");

    // Get username
    printf("Enter Username: ");
    scanf("%s", username);

    // Get password
    printf("Enter Password: ");
    scanf("%s", password);

    // Validate credentials
    if (strcmp(username, stored_username) == 0 && strcmp(password, stored_password) == 0) {
        printf("Login successful! Starting the game...\n\n");

        // Proceed to the game (call your game code here)
        // Example: game(); // Uncomment when integrated
    } else {
        printf("Invalid username or password. Please try again.\n");
    }

    return 0;
}
